name: "firebase_sync_hive"
description: "A starting point for Dart libraries or applications."

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  firebase_database_rest: ^1.0.2
  firebase_sync: ^1.0.0
  hive: ^2.0.0
  meta: ^1.3.0
  synchronized: ^3.0.0

dependency_overrides:
  firebase_sync:
    path: "../firebase_sync"

dev_dependencies:
  coverage: ^1.0.2
  dart_pre_commit: ^2.2.0-nullsafety.2
  lint: ^1.5.3
  mocktail: ^0.1.1
  test: ^1.16.8
  tuple: ^2.0.0
